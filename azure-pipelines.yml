trigger:
- main

pool:
  vmImage: 'windows-latest'

steps:
- checkout: self

- script: |
    echo "Listing files..."
    dir
  displayName: "Check files"

- script: |
    echo "Running app.js"
    node app.js
  displayName: "Run JavaScript"
